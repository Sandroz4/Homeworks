// // 4. You will be presented with a list of names. The task is to bring it back 
// // New list where you will have only those names that start with capital 
// // with a letter and their length does not exceed 5.



// const names = ['James', 'sandro', 'Giorgi', 'Jangira', 'Cristiano'];

// function capitalizedAndFive(names) {
//     return names.filter(function(name) {
//         return name.charAt(0) === name.charAt(0).toUpperCase() && name.length === 5;
//     });
// }

// let capitalizedAndFiltered = capitalizedAndFive(names);
// console.log(capitalizedAndFiltered);
